{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/verify-wezterm-backup.sh\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Output ONLY valid JSON, no other text.\n\nDid this session involve WezTerm work (editing .wezterm.lua, troubleshooting, or configuration)?\n\nYES: {\"ok\": false, \"reason\": \"Before ending, ask the user if they have any WezTerm learnings to capture. If they do, append to ${CLAUDE_PLUGIN_ROOT}/.cache/learnings.md with [YYYY-MM-DD] prefix under 'Successful Patterns' or 'Mistakes to Avoid'.\"}\n\nNO: {\"ok\": true}"
          }
        ]
      }
    ]
  }
}
