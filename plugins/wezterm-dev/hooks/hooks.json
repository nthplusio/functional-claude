{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/verify-wezterm-backup.sh\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if ${CLAUDE_PLUGIN_ROOT}/.cache/learnings.md exists. If missing or last_refresh in frontmatter is not today, use WebFetch to gather current WezTerm info from: (1) https://wezfurlong.org/wezterm/config/lua/config/index.html, (2) https://github.com/wez/wezterm/releases, (3) https://wezfurlong.org/wezterm/config/lua/wezterm/. Update the Reference Cache section with today's date, preserving existing Learnings section. Create the file if it doesn't exist using the template structure with YAML frontmatter. Note: The .cache directory is gitignored and should not be committed."
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Evaluate if this session involved WezTerm configuration work (editing .wezterm.lua, troubleshooting WezTerm issues, or discussing WezTerm configuration). If YES: ask the user if there are learnings to capture, and if they provide any, append to ${CLAUDE_PLUGIN_ROOT}/.cache/learnings.md Learnings section with date prefix [YYYY-MM-DD] under categories 'Successful Patterns' or 'Mistakes to Avoid', then return {\"ok\": true}. If NO WezTerm work was done: return ONLY {\"ok\": true} with no other text."
          }
        ]
      }
    ]
  }
}
