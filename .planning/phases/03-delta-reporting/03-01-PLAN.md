---
phase: 03-delta-reporting
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - plugins/project-manager/hooks/lib/cache-store.js
  - plugins/project-manager/hooks/lib/cache-store.test.js
  - plugins/project-manager/skills/pm-status/SKILL.md
autonomous: true
requirements: [SYNC-05]

must_haves:
  truths:
    - "After delta sync, plugin displays a change summary listing each changed issue with field-level diffs"
    - "Status changes show old -> new value (e.g., 'started -> completed')"
    - "Priority changes show labels not numbers (e.g., 'High -> Urgent')"
    - "New issues (in delta but not in cache) are marked as new"
    - "When no issues changed since last sync, the plugin reports 'No changes since last sync'"
    - "Only user-visible fields are diffed: status, priority, assignee, title (not description, updatedAt)"
  artifacts:
    - path: "plugins/project-manager/hooks/lib/cache-store.js"
      provides: "diffIssues and formatChangeSummary functions"
      exports: ["diffIssues", "formatChangeSummary"]
    - path: "plugins/project-manager/hooks/lib/cache-store.test.js"
      provides: "Unit tests for diffIssues and formatChangeSummary"
      contains: "describe.*diffIssues"
    - path: "plugins/project-manager/skills/pm-status/SKILL.md"
      provides: "Step 4 updated to call diffIssues before mergeIssues and output change summary"
      contains: "diffIssues"
  key_links:
    - from: "plugins/project-manager/skills/pm-status/SKILL.md"
      to: "plugins/project-manager/hooks/lib/cache-store.js"
      via: "cs.diffIssues(existing.issues, delta.issues) called before cs.mergeIssues"
      pattern: "diffIssues.*existing.*delta"
    - from: "plugins/project-manager/skills/pm-status/SKILL.md"
      to: "plugins/project-manager/hooks/lib/cache-store.js"
      via: "cs.formatChangeSummary(changes) for display output"
      pattern: "formatChangeSummary"
---

<objective>
Add delta reporting to the project-manager plugin so users see exactly what changed since their last session -- which issues moved, what fields updated -- instead of a silent merge.

Purpose: Completes SYNC-05, the final v1 requirement. After delta sync, users get an actionable change summary (e.g., "NTH-42: started -> completed") instead of just refreshed counts.

Output: Two new exports in cache-store.js (`diffIssues`, `formatChangeSummary`), comprehensive tests, and pm-status skill Step 4 updated to display change summaries.
</objective>

<execution_context>
@/home/scoussens/.claude/get-shit-done/workflows/execute-plan.md
@/home/scoussens/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-delta-reporting/03-RESEARCH.md
@.planning/phases/03-delta-reporting/03-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From plugins/project-manager/hooks/lib/cache-store.js (current exports):
```javascript
module.exports = {
  getCacheDir,
  readIssues,
  writeIssues,
  readSyncMeta,
  writeSyncMeta,
  mergeIssues,      // { ...existingData.issues, ...deltaResult.issues } -- blind merge, no diff
  classifyFreshness,
  formatAge,
  CACHE_VERSION,
  CACHE_ROOT,
};
```

NormalizedIssue schema (flat, all fields are primitives):
```javascript
{
  id: 'NTH-42',           // string
  title: 'Auth refactor',  // string
  status: 'started',       // string: backlog|unstarted|started|completed|cancelled
  priority: 2,             // number: 0=none, 1=urgent, 2=high, 3=medium, 4=low
  assignee: 'Scott',       // string|null
  description: '...',      // string (truncated to 500 chars)
  updatedAt: '2026-...',   // ISO string
  tracker: 'linear',       // string: linear|github
}
```

mergeIssues signature (the companion function for diffIssues):
```javascript
function mergeIssues(existingData, deltaResult) {
  // existingData: { version, tracker, syncedAt, issues: { [id]: NormalizedIssue } }
  // deltaResult: { syncedAt, issues: { [id]: NormalizedIssue } }
  const mergedIssues = { ...existingData.issues, ...deltaResult.issues };
  return { version: existingData.version, tracker: existingData.tracker, syncedAt: deltaResult.syncedAt, issues: mergedIssues };
}
```

Test patterns (node:test + node:assert/strict, 37 existing tests):
```javascript
const { describe, it, beforeEach, afterEach } = require('node:test');
const assert = require('node:assert/strict');
// Tests use sampleData() helper returning { version: CACHE_VERSION, tracker, syncedAt, issues: { 'NTH-42': {...} } }
```

pm-status SKILL.md Step 4 current output (GitHub example):
```javascript
console.log(JSON.stringify({ deltaCount: Object.keys(delta.issues).length, totalCount: Object.keys(merged.issues).length }));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD diffIssues and formatChangeSummary in cache-store</name>
  <files>plugins/project-manager/hooks/lib/cache-store.js, plugins/project-manager/hooks/lib/cache-store.test.js</files>
  <behavior>
    diffIssues(existingIssues, deltaIssues):
    - Returns empty array when deltaIssues is empty object
    - Returns empty array when existingIssues is empty and deltaIssues is empty
    - Marks new issues (in delta but not in existing) with type: 'new', empty fields array
    - Detects status field change: { field: 'status', from: 'started', to: 'completed' }
    - Detects priority field change: { field: 'priority', from: 2, to: 1 }
    - Detects assignee field change: { field: 'assignee', from: 'Scott', to: 'Jane' }
    - Detects title field change: { field: 'title', from: 'Old', to: 'New' }
    - Detects multiple field changes on same issue (returns all changed fields)
    - Omits issues where only updatedAt changed (no user-visible field diff)
    - Does NOT diff description or updatedAt fields (not in DIFF_FIELDS)

    formatChangeSummary(changes):
    - Returns 'No changes since last sync' for empty array
    - Formats updated issue: 'NTH-42 Auth refactor: started -> completed'
    - Formats new issue: 'NTH-105 New dashboard layout (new)'
    - Formats priority with labels: 'priority High -> Urgent' (not raw numbers)
    - Formats multiple field changes on one issue as separate lines or combined
    - Includes count line: 'N issue(s) changed'
    - Handles assignee null -> value: 'assignee (unassigned) -> Scott'
  </behavior>
  <action>
    **RED phase:**
    1. Read cache-store.test.js immediately before editing
    2. Add a `describe('diffIssues', ...)` block with these tests:
       - `it('returns empty array when deltaIssues is empty')` -- diffIssues({ 'NTH-1': {...} }, {}) => []
       - `it('marks new issues not in existing cache')` -- diffIssues({}, { 'NTH-1': {...} }) => [{ id, type: 'new', title, fields: [] }]
       - `it('detects status field change')` -- existing started, delta completed => fields[0] = { field: 'status', from: 'started', to: 'completed' }
       - `it('detects priority field change')` -- existing 2, delta 1 => { field: 'priority', from: 2, to: 1 }
       - `it('detects assignee field change')` -- existing 'Scott', delta 'Jane'
       - `it('detects title field change')` -- existing 'Old title', delta 'New title'
       - `it('detects multiple field changes on same issue')` -- change both status and priority => fields.length === 2
       - `it('omits issues where only updatedAt changed')` -- identical visible fields, different updatedAt => result.length === 0
       - `it('does not diff description field')` -- different descriptions, same visible fields => result.length === 0
    3. Add a `describe('formatChangeSummary', ...)` block with these tests:
       - `it('returns no-changes message for empty array')` -- formatChangeSummary([]) => 'No changes since last sync'
       - `it('formats status change with field values')` -- { type: 'updated', fields: [{ field: 'status', from: 'started', to: 'completed' }] } => includes 'started -> completed'
       - `it('formats new issue')` -- { type: 'new', title: 'New layout' } => includes '(new)'
       - `it('formats priority with labels not numbers')` -- { field: 'priority', from: 2, to: 1 } => includes 'High -> Urgent'
       - `it('formats assignee null as unassigned')` -- { field: 'assignee', from: null, to: 'Scott' } => includes '(unassigned) -> Scott'
       - `it('includes issue count line')` -- 2 changes => includes '2 issue(s) changed'
    4. Add diffIssues and formatChangeSummary to the require destructuring at top of test file
    5. Run tests: `node --test plugins/project-manager/hooks/lib/cache-store.test.js` -- all new tests MUST fail (functions not yet implemented)
    6. Commit RED: `test(03-01): add failing tests for diffIssues and formatChangeSummary`

    **GREEN phase:**
    1. Read cache-store.js immediately before editing
    2. Add `diffIssues(existingIssues, deltaIssues)` function after `mergeIssues`:
       - `const DIFF_FIELDS = ['status', 'priority', 'assignee', 'title'];`
       - Iterate Object.entries(deltaIssues)
       - If id not in existingIssues: push { id, type: 'new', title: deltaIssue.title, fields: [] }
       - Otherwise: compare each DIFF_FIELDS value with strict !==, collect changed fields
       - If fields.length > 0: push { id, type: 'updated', title: deltaIssue.title, fields }
       - Return changes array
    3. Add `formatChangeSummary(changes)` function after `diffIssues`:
       - Priority label map: `{ 0: 'None', 1: 'Urgent', 2: 'High', 3: 'Medium', 4: 'Low' }`
       - If changes.length === 0: return 'No changes since last sync'
       - Build lines array starting with 'CHANGES SINCE LAST SYNC'
       - For each change:
         - If type === 'new': `  ${id} ${title} (new)`
         - If type === 'updated': for each field, format the change line:
           - For priority field: use label map for from/to values
           - For assignee field: use '(unassigned)' for null values
           - Format: `  ${id} ${title}: ${fieldLabel} ${fromFormatted} -> ${toFormatted}`
           - For status field, omit field label (it is the primary change): `  ${id} ${title}: ${from} -> ${to}`
       - Append count line: `\n${changes.length} issue(s) changed`
       - Return joined string
    4. Add both functions to module.exports
    5. Run tests: `node --test plugins/project-manager/hooks/lib/cache-store.test.js` -- ALL tests must pass (37 existing + new)
    6. Commit GREEN: `feat(03-01): implement diffIssues and formatChangeSummary`

    **IMPORTANT:**
    - diffIssues takes `existingIssues` and `deltaIssues` (the .issues objects, NOT the full cache data objects) -- this matches how mergeIssues takes existingData/deltaResult but diffIssues operates one level deeper
    - Call diffIssues BEFORE mergeIssues (after merge, before-state is lost)
    - Do NOT diff description or updatedAt -- description is noisy, updatedAt always changes for delta issues
    - Priority labels in formatChangeSummary (display layer), NOT in diffIssues (data layer)
  </action>
  <verify>
    <automated>node --test plugins/project-manager/hooks/lib/cache-store.test.js</automated>
  </verify>
  <done>
    - diffIssues and formatChangeSummary exported from cache-store.js
    - All existing 37 tests still pass (zero regressions)
    - New diffIssues tests pass: empty delta, new issue, status/priority/assignee/title changes, multi-field, updatedAt-only omission, description-not-diffed
    - New formatChangeSummary tests pass: no-changes message, status format, new issue format, priority labels, null assignee, count line
  </done>
</task>

<task type="auto">
  <name>Task 2: Update pm-status skill Step 4 to output change summary</name>
  <files>plugins/project-manager/skills/pm-status/SKILL.md</files>
  <action>
    Read plugins/project-manager/skills/pm-status/SKILL.md immediately before editing.

    Modify Step 4 (Delta Sync) to call diffIssues before mergeIssues and output the change summary.

    **For GitHub projects** -- update the node one-liner to:
    1. Read existing cache: `const existing = cs.readIssues(slug);`
    2. Fetch delta: `const delta = gh.fetchDelta({ repoKey: '<org/repo>' }, meta.lastSyncedAt);`
    3. Diff BEFORE merge: `const changes = cs.diffIssues(existing.issues, delta.issues);`
    4. Merge: `const merged = cs.mergeIssues(existing, delta);`
    5. Write cache and meta (same as current)
    6. Output: `console.log(JSON.stringify({ changes, changeSummary: cs.formatChangeSummary(changes), deltaCount: Object.keys(delta.issues).length, totalCount: Object.keys(merged.issues).length }));`

    **For Linear projects** -- same pattern in the Linear node one-liner:
    1. After normalizeLinearDelta, before mergeIssues: `const changes = cs.diffIssues(existing.issues, delta.issues);`
    2. Update output to include `changes` and `changeSummary`

    **Add display instruction** after Step 4 code blocks:
    After running the delta sync one-liner, check the JSON output:
    - If `changeSummary` is present and not "No changes since last sync", display it before proceeding to Step 6
    - If `changeSummary` is "No changes since last sync", display that line before proceeding to Step 6
    - This ensures the user always sees what changed (or that nothing changed) after a delta sync

    **Do NOT modify Steps 1-3, 5-7** -- they are unrelated to delta reporting.
  </action>
  <verify>
    <automated>grep -c 'diffIssues' plugins/project-manager/skills/pm-status/SKILL.md</automated>
  </verify>
  <done>
    - pm-status SKILL.md Step 4 calls cs.diffIssues(existing.issues, delta.issues) BEFORE cs.mergeIssues in both GitHub and Linear code blocks
    - Output JSON includes changes array and changeSummary string
    - Skill instructs model to display the changeSummary to the user after delta sync
    - Steps 1-3 and 5-7 are unchanged
  </done>
</task>

</tasks>

<verification>
1. `node --test plugins/project-manager/hooks/lib/cache-store.test.js` -- all tests pass (37 existing + ~15 new)
2. `node --test plugins/project-manager/hooks/lib/*.test.js` -- full suite green, zero regressions
3. `grep 'diffIssues\|formatChangeSummary' plugins/project-manager/hooks/lib/cache-store.js` -- both functions exist
4. `grep 'diffIssues' plugins/project-manager/skills/pm-status/SKILL.md` -- Step 4 references diffIssues
5. `node -e "const cs = require('./plugins/project-manager/hooks/lib/cache-store'); console.log(typeof cs.diffIssues, typeof cs.formatChangeSummary)"` -- outputs "function function"
</verification>

<success_criteria>
- diffIssues correctly identifies new issues, field-level changes (status, priority, assignee, title), and omits non-meaningful changes
- formatChangeSummary produces human-readable output with priority labels (not numbers), null-safe assignee display, and explicit "no changes" messaging
- pm-status skill Step 4 calls diffIssues before mergeIssues and outputs the formatted change summary
- All existing 37+ tests pass with zero regressions
- SYNC-05 requirement is satisfied: delta sync produces a change summary showing which issues changed and what fields moved
</success_criteria>

<output>
After completion, create `.planning/phases/03-delta-reporting/03-01-SUMMARY.md`
</output>
