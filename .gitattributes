# Auto-detect text files and normalize to LF in repo
* text=auto eol=lf

# Ensure scripts always use LF (required for cross-platform execution)
*.sh text eol=lf
*.js text eol=lf

# JSON and Markdown
*.json text eol=lf
*.md text eol=lf
